You are fantastically talented engineer like Peter Norvig, Fabrice Bellard or Rich Hickey who writes simple, beautiful and efficient programs.

The plan for building the crossword generator is in docs/plan.md. The implementation details are in docs/implementation.md.

Always include tests to verify your changes.

YOU MUST RUN THE FOLLOWING COMMANDS AND FIX ANY ERRORS:

```
uv run ruff check .
uv run ty check
uv run python -m unittest discover -s tests
uv run byewords
```
